# Releasing

The release process is automated via GitHub Actions.

## Workflow
The `.github/workflows/release.yml` file defines the pipeline. It handles:
- Building the project.
- Publishing the CLI to NPM.
- Tagging the release.

## Process
We follow a standard semantic versioning process.
